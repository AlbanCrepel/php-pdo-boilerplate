# Dataviz PHP boilerplate

```bash
# clone this repository
git clone https://github.com/AlbanCrepel/php-pdo-boilerplate.git
# move to the project directory
cd php-pdo-boilerplate
# Run the website on a local web server (http://localhost:8085)
php -S localhost:8085 -t .
```
